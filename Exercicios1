using System;
using System.Globalization;

/*
Faça um programa para ler dois valores inteiros, e depois mostrar na tela a soma desses números com uma 
mensagem explicativa, conforme exemplos.

namespace Exercicio1
{
    class Program
    {
        static void Main(string[] args) 
        {
            int a = int.Parse(Console.ReadLine()); 
            int b = int.Parse(Console.ReadLine());
            int soma = a + b;

            Console.WriteLine(soma);
        }
    }
}
*/

/*
Faça um programa para ler o valor do raio de um círculo, e depois mostrar o valor da área deste círculo com quatro 
casas decimais conforme exemplos.
Fórmula da área: area = π . raio2
Considere o valor de π = 3.14159

namespace Exercicio2
{
    class Program
    {
        static void Main(string[] args)
        {
            double raio = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);
            double area = Math.Pow(raio,2)*3.14159;
            Console.WriteLine(area.ToString("F4"));

        }
    }
}
*/

/*
Fazer um programa para ler quatro valores inteiros A, B, C e D. A seguir, calcule e mostre a diferença do produto 
de A e B pelo produto de C e D segundo a fórmula: DIFERENCA = (A * B - C * D).

namespace Exercicio3
{
    class Program
    {
        static void Main(string[] args)
        { 
            int a = int.Parse(Console.ReadLine());
            int b = int.Parse(Console.ReadLine());
            int c = int.Parse(Console.ReadLine());
            int d = int.Parse(Console.ReadLine());

            int diferenca = (a*b - c*d);

            Console.WriteLine(diferenca);
        }
    }
}
*/

/*
Fazer um programa que leia o número de um funcionário, seu número de horas trabalhadas, o valor que recebe por 
hora e calcula o salário desse funcionário. A seguir, mostre o número e o salário do funcionário, com duas casas 
decimais.

namespace Exercicio4
{
    class Program
    {
        static void Main(string[] args) 
        {
            Console.WriteLine("Insira o seu numero de funcionário:");
            int numFuncionarios = int.Parse(Console.ReadLine());
            Console.WriteLine("Insira a quantia de horas trabalhadas: ");
            int horasTrabalhadas = int.Parse(Console.ReadLine());
            Console.WriteLine("Valor recebido por hora trabalhada:");
            double salarioHora = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);

            salarioHora = (double)horasTrabalhadas * salarioHora;

            Console.WriteLine("Number: {0} ", numFuncionarios);
            Console.WriteLine("Salary: U$ {0}", salarioHora.ToString("F2")) ;

        }
    }
}*/


/*
Fazer um programa para ler o código de uma peça 1, o número de peças 1, o valor unitário de cada peça 1, o 
código de uma peça 2, o número de peças 2 e o valor unitário de cada peça 2. Calcule e mostre o valor a ser pago.

namespace Exercicio5
{
    class Program
    {
        static void Main(string[] args)
        {
            string[] peca1 = Console.ReadLine().Split(' ');
            int codigoPeca1 = int.Parse(peca1[0]);
            int quantiaPeca1 = int.Parse(peca1[1]);
            double valorPeca1 = double.Parse(peca1[2], CultureInfo.InvariantCulture);

            string[] peca2 = Console.ReadLine().Split(' ');
            int codigoPeca2 = int.Parse(peca2[0]);
            int quantiaPeca2 = int.Parse(peca2[1]);
            double valorPeca2 = double.Parse(peca2[2], CultureInfo.InvariantCulture);

            double precoFinal = (valorPeca1 * quantiaPeca1 + valorPeca2 * quantiaPeca2);

            Console.WriteLine("Valor a pagar: R$ {0}", precoFinal.ToString("F2"));


        }
    }
}
*/


/*
Fazer um programa que leia três valores com ponto flutuante de dupla precisão: A, B e C. Em seguida, calcule e 
mostre: 
a) a área do triângulo retângulo que tem A por base e C por altura. 
b) a área do círculo de raio C. (pi = 3.14159) 
c) a área do trapézio que tem A e B por bases e C por altura. 
d) a área do quadrado que tem lado B. 
e) a área do retângulo que tem lados A e B

namespace Exercicio6
{
    class Program
    {
        static void Main(string[] args)
        {
            string[] vet = Console.ReadLine().Split(' ');
            double a = double.Parse(vet[0], CultureInfo.InvariantCulture);
            double b = double.Parse(vet[1], CultureInfo.InvariantCulture);
            double c = double.Parse(vet[2], CultureInfo.InvariantCulture);

            double areaTriangulo = (a * c) / 2;
            double areaCirculo = Math.Pow(c, 2) * 3.14159;
            double areaTrapezio = ((a + b) * c) / 2;
            double areaQuadrado = b * b;
            double areaRetangulo = a * b;

            Console.WriteLine("Triângulo: {0}", areaTriangulo.ToString("F3"));
            Console.WriteLine("Círculo: {0}", areaCirculo.ToString("F3"));
            Console.WriteLine("Trapézio: {0}", areaTrapezio.ToString("F3"));
            Console.WriteLine("Quadrado: {0}", areaQuadrado.ToString("F3"));
            Console.WriteLine("Retangulo: {0}", areaRetangulo.ToString("F3"));

        }
    }
}
*/
